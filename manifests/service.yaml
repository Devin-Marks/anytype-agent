apiVersion: v1
kind: Service
metadata:
  name: anytype-agent
  namespace: anytype
  labels:
    app.kubernetes.io/name: anytype-agent
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: anytype-agent
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
